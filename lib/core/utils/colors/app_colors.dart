import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  // --- Brand Core Colors ---
  /// Warm Coral (#FF7F4A / #FF784A) - Primary Accent
  static const Color warmCoral = Color(0xFFFF7F4A);

  // warm pink
  static const Color warmPink = Color(0xFFFF6B6B);

  /// Golden Honey (#FFB800) - Secondary Accent
  static const Color goldenHoney = Color(0xFFFFB800);

  /// Fresh Mint (#3EC4B6) - Secondary Accent
  static const Color freshMint = Color(0xFF3EC4B6);

  // --- Neutrals & Backgrounds ---
  /// Off-White (#FAFAFA) - App Background
  static const Color offWhite = Color(0xFFFAFAFA);

  /// Dark Brand Brown (#3D2314) - Primary Text & Logo Dark Tone
  static const Color darkBrown = Color(0xFF3D2314);

  // --- Light Accent Tints (For Badges & Containers) ---
  static const Color lightCoral = Color(0xFFFFF0EC);
  static const Color lightHoney = Color(0xFFFFF8E6);
  static const Color lightMint = Color(0xFFEBF9F8);

  /// Card & Container Background
  static const Color white = Color(0xFFFFFFFF);

  /// Black (#000000) - Text Color
  static const Color black = Color(0xFF000000);

  /// Border Gray (#E2E8F0) - Border Color
  static const Color borderGray = Color(0xFFE2E8F0);

  // ===========================================================
  // SUCCESS
  // ===========================================================

  static const Color success = Color(0xFF10B981);

  // ===========================================================
  // WARNING
  // ===========================================================

  static const Color warning = Color(0xFFF59E0B);

  // ===========================================================
  // ERROR
  // ===========================================================

  static const Color error = Color(0xFFEF4444);

  // ===========================================================
  // INFO
  // ===========================================================

  static const Color info = Color(0xFF3B82F6);
}
